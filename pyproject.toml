[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "pooper"
version = "0.1.0"
description = "NGINX analyzer"
requires-python = ">=3.14.6"
dependencies = [
    "numpy>=2.5.0",
    "matplotlib>=3.11.0"
]
